import { AuthFormIntegrated as AuthForm } from "@/components/storefront/auth-form-integrated";
export const metadata = { title: "Đặt lại mật khẩu" };
export default function ResetPage() { return <AuthForm mode="reset"/>; }
