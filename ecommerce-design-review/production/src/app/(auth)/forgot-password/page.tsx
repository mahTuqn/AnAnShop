import { AuthFormIntegrated as AuthForm } from "@/components/storefront/auth-form-integrated";
export const metadata = { title: "Quên mật khẩu" };
export default function ForgotPage() { return <AuthForm mode="forgot"/>; }
