import { StorefrontShell } from "@/components/storefront/site-shell-v2";

export default function StorefrontLayout({ children }: { children: React.ReactNode }) { return <StorefrontShell>{children}</StorefrontShell>; }
