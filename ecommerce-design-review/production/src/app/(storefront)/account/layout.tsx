import { AccountFrame } from "@/components/storefront/account-frame";
export default function AccountLayout({ children }: { children: React.ReactNode }) { return <AccountFrame>{children}</AccountFrame>; }
