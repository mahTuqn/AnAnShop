import Link from "next/link";
import { money } from "@/lib/storefront/data";
import type { Product } from "@/lib/storefront/types";

export function ProductCard({ product }: { product: Product }) {
  return <article className="group relative"><Link href={`/products/${product.slug}`} className="block rounded-3xl focus-visible:outline-none focus-visible:ring-2 focus-visible:ring-[#8a493d]"><span className="relative block aspect-[4/5] overflow-hidden rounded-3xl bg-[#eee6e1]"><img className="size-full object-cover transition duration-500 group-hover:scale-[1.03]" src={product.image} alt={product.name}/>{product.badge && <span className="absolute left-3 top-3 rounded-full bg-white/90 px-3 py-1 text-xs font-semibold text-[#713c33]">{product.badge}</span>}</span><span className="mt-4 block"><span className="text-xs uppercase tracking-[.16em] text-[#8a7d77]">{product.categoryLabel}</span><span className="mt-1 block min-h-12 font-medium leading-6">{product.name}</span><span className="mt-1 flex flex-wrap gap-2"><strong>{money(product.price)}</strong>{product.compareAtPrice && <del className="text-sm text-[#9b918c]">{money(product.compareAtPrice)}</del>}</span><span className="mt-2 block text-sm text-[#776b65]">★ {product.rating} ({product.reviewCount})</span></span></Link><button className="absolute right-3 top-3 grid size-10 place-items-center rounded-full bg-white/90 text-xl shadow-sm hover:text-[#9a4e58]" aria-label={`Thêm ${product.name} vào yêu thích`}>♡</button></article>;
}
