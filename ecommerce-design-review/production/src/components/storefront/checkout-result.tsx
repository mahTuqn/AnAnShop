import { ButtonLink } from "@/components/ui/button";
export function CheckoutResult({ success, code }: { success: boolean; code: string }) { return <main className="mx-auto max-w-2xl px-4 py-24 text-center"><span className={`mx-auto grid size-20 place-items-center rounded-full text-4xl ${success ? "bg-green-100 text-green-700" : "bg-red-100 text-red-700"}`} aria-hidden>{success ? "✓" : "!"}</span><p className="mt-7 text-xs font-semibold tracking-[.2em] text-[#c26b77]">{success ? "ĐƠN HÀNG ĐÃ ĐƯỢC GHI NHẬN" : "THANH TOÁN CHƯA HOÀN TẤT"}</p><h1 className="mt-4 font-serif text-4xl">{success ? "Cảm ơn mẹ đã tin chọn An An." : "Mình thử lại một lần nữa nhé."}</h1><p className="mx-auto mt-5 max-w-lg leading-7 text-[#6b5e5e]">{success ? <>Đơn <strong>#{code}</strong> đang chờ xác nhận. An An sẽ gửi cập nhật qua email và số điện thoại của mẹ.</> : <>Đơn <strong>#{code}</strong> vẫn được giữ trong 15 phút. Tài khoản chưa bị trừ tiền.</>}</p><div className="mt-9 flex flex-wrap justify-center gap-3"><ButtonLink href={success ? `/account/orders/${code}` : "/checkout"}>{success ? "Theo dõi đơn hàng" : "Thử thanh toán lại"}</ButtonLink><ButtonLink href="/products" variant="secondary">Tiếp tục mua sắm</ButtonLink></div></main>; }
